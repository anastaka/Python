def animals(heads, legs):
    x = heads - (legs - 2*heads)/2
    y = (legs - 2*heads)/2
    if x.is_integer () and y.is_integer () and x >= 0 and y >= 0:
            return (x, y)
    else:
        return "No solutions"
