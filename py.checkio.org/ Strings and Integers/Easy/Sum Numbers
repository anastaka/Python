def sum_numbers(text: str) -> int:
    num = []
    text = text.split()
    for n in text:
        if n.isdigit() == True:
            num.append(n)
        else:
            num.append(0)
    return (sum(int(p) for p in num))
