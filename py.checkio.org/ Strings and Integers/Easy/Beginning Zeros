def beginning_zeros(a: str) -> int:
    if a[0] != '0':
        return (0)
    else:
        for i in range(len(a)): 
            if a[i] == '0':
                i += 1
            else:
                break
        return i
